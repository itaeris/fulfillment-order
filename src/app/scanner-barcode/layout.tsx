import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Validasi scan - Aeris Beaute",
};

export default function ScannerBarcodeLayout({ children }: { children: React.ReactNode }) {
  return children;
}
